{
  "name": "rocket-queen",
  "version": "1.0.0",
  "private": true,
  "description": "Aviator crash game for Telegram Mini Apps",
  "scripts": {
    "dev": "npx --yes concurrently -n server,client -c magenta,cyan \"npm run dev --prefix server\" \"npm run dev --prefix client\"",
    "dev:server": "npm run dev --prefix server",
    "dev:client": "npm run dev --prefix client",
    "start": "npm run start --prefix server",
    "build": "npm run build --prefix client"
  }
}
